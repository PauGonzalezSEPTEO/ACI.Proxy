services:
  mssql:
    image: mcr.microsoft.com/mssql/server:2022-latest
    user: root
    container_name: mssql
    command: /bin/bash /entrypoint.sh
    ports:
      - 1433:1433
    environment:
      - ACCEPT_EULA=Y
      - MSSQL_SA_PASSWORD=.acisa159753
    volumes:
      - dbdata:/var/opt/mssql/data
      - ./db/mssql/docker-entrypoint.sh:/entrypoint.sh
      - ./db/mssql/docker-db-init.sh:/db-init.sh
      - ./db/mssql/mssql-proxy.sql:/db-init.sql
    expose:
      - "1433"
    healthcheck:
      test: /opt/mssql-tools18/bin/sqlcmd -C -S localhost -U sa -P ".acisa159753" -Q "SELECT 1" -b -o /dev/null
      interval: 10s
      timeout: 3s
      retries: 10
      start_period: 10s

  ACI.Proxy.api:
    image: ${DOCKER_REGISTRY-}aciproxyapi
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 5000:80
    environment:
      - Serilog:WriteTo:0:Args:path=/app/log.txt
    depends_on:
      mssql:
        condition: service_healthy
    expose:
      - "5000"
    volumes:
      - ./certificates/certificate.pfx:/app/certificates/certificate.pfx
      - ./certificates/key-4937e7c0-d16d-4455-aa72-cdce6f38b597.xml:/app/certificates/key-4937e7c0-d16d-4455-aa72-cdce6f38b597.xml 

volumes:
  dbdata:
